
_ less than 5,4 3,2 => HARD CODE IT


_ find position of the max (stack_b)
	if the max in the first half : rb untill he becomes at the top
	else: rrb untill he becomes a the top
	then: pa


postion_max = find_max() => returns position of the max in int the stack_b

if postion_max < size_b / 2
	while (postion_max--)
		rb;
else if postion_max >= size / 2
	while (postion_max++ <= size)
		rrb
pa()	

