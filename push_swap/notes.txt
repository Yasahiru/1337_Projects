
_ less than 5,4 3,2 => HARD CODE IT
_ in case 100 or less elements: 
	start = 0
	end = size/6
	_ check if the index of the first element (start >= rank)
		pb 
		start --
		end --
		rb
	_ else if (rank >= end):
		start --
		end --
		push to stack b

	else 
	rra

_ additional functions: 
	if is sorted => is _sorted()
	indexing() => buble sort  !!!!!!!!!!!!!!!!!!!!!!!!

_ push to a => (find max) if its in the first half rb . if its at the botton rrb then pa.